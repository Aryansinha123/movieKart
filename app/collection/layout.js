export const metadata = {
  title: "My Collections",
  description:
    "Organize your favorite movies into curated collections on MovieKart. Create public or private lists and share them with friends.",
};

export default function CollectionLayout({ children }) {
  return children;
}
