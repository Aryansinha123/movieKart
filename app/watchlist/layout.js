export const metadata = {
  title: "My Watchlist",
  description:
    "Movies saved to watch later on MovieKart. Never lose track of what to watch next.",
};

export default function WatchlistLayout({ children }) {
  return children;
}
