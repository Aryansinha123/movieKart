export const metadata = {
  title: "Settings",
  description: "Manage your MovieKart account settings and preferences.",
  robots: { index: false, follow: false },
};

export default function SettingsLayout({ children }) {
  return children;
}
