export const metadata = {
  title: "Collections",
  description: "Discover curated movie and TV collections on MovieKart — save to your library and track progress.",
};

export default function CollectionsLayout({ children }) {
  return children;
}
