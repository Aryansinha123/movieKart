export const metadata = {
  title: "Watched Movies",
  description:
    "Movies you've already watched, tracked and organized on MovieKart.",
};

export default function WatchedLayout({ children }) {
  return children;
}
