export const metadata = {
  title: "Login",
  description:
    "Sign in to MovieKart to access your personalized AI movie recommendations, collections, and watchlist.",
};

export default function LoginLayout({ children }) {
  return children;
}
